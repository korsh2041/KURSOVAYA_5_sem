# Система управления библиотекой (курсовая)

Версия проекта, очищенная под курсовую работу: оставлен только функционал, необходимый для демонстрации ключевых сценариев библиотеки
(пользователи/роли → каталог книг → выдача/возврат → базовые отчеты) и аккуратной архитектуры.

## ✅ Что осталось в проекте

- **Пользователи и роли:** Student / Professor / Librarian
- **Регистрация и вход** (REST)
- **Каталог книг**
  - типы: PrintedBook / EBook / Magazine
  - поиск по критериям (Factory Method)
  - копии книг (Prototype)
- **Выдача и возврат**
  - учет лимитов
  - состояния выдачи (State)
- **Уведомления** (Observer) — в виде текстовых сообщений/логики внутри приложения
- **Отчеты** (Strategy) — базовые стратегии отчетности
- **Хранение данных:** PostgreSQL через Spring Data JPA + миграции Flyway
- **Web UI (статические страницы):** login/register + страницы по ролям (в `src/main/resources/static`)

## 🧩 Паттерны проектирования (оставленные)

1. **Singleton** — `Library`
2. **Prototype** — `BookCopy`
3. **Factory Method** — `SearchCriteriaFactory`
4. **Template Method** — `User`
5. **Observer** — `NotificationService`
6. **State** — `LoanState`
7. **Strategy** — стратегии отчетов (`library/reports`)
8. **Facade** — `AuthenticationFacade`
9. **Proxy** — `UserAccountProxy`

## ❌ Что удалено (как избыточное для курсовой)

- платежные адаптеры/интеграции
- рейтинги/композитная система отзывов
- декораторы книг
- история действий (memento)
- поставки/инвентарь и смены
- файловые/памятные репозитории и вспомогательные FileUtils
- старый `Main.java` и собранные `target/`

## 🚀 Запуск

### 1) Настройка БД
В `src/main/resources/application.properties` укажи доступ к PostgreSQL.

### 2) Запуск Spring Boot
```bash
mvn spring-boot:run
```

После запуска:
- статические страницы доступны по адресу: `http://localhost:8080/login.html` (и другие .html)
- REST API: `/api/...`

## 📌 Примечание
Проект рассчитан на Java 11 и Spring Boot 2.7.x.
